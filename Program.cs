using System;

namespace ProjetApplication
{
    class Program
    {
        static void Main(string[] args)
        {
            // TEST 
            // Pizzeria pizzeria = new Pizzeria();
            // Client c = new Client(1, "John", "Doe", 1, "0606060606", DateTime.Now);
            // Client c1 = new Client(12, "encule", "Doe", 1, "123", DateTime.Now);
            // pizzeria.addClient(c);

            // Help_cooker help_cooker = new Help_cooker();
            // help_cooker.isFirstTimeOrder(c, 1, pizzeria);
            // help_cooker.isFirstTimeOrder(c1, 1, pizzeria);
            // Console.Write("\n");
        }
    }
}